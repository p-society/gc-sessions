export enum PenaltyActionType {
  FOUL = 'foul',
  YELLOW_CARD = 'yellow_card',
  RED_CARD = 'red_card',
  PENALTY_KICK = 'penalty_kick',
}

export enum PenaltyType {
  FREE_KICK = 'free_kick',
  WARNING = 'warning',
  PLAYER_SENT_OFF = 'player_sent_off',
  DIRECT_GOAL_ATTEMPT = 'direct_goal_attempt',
}

export enum ExtraActionType {
  CORNER_KICK = 'corner_kick',
  THROW_IN = 'throw_in',
  GOAL_KICK = 'goal_kick',
}

export enum OpportunityType {
  GOAL_ATTEMPT = 'goal_attempt',
  RESTART_PLAY = 'restart_play',
}
